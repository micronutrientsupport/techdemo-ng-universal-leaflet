language: node_js
node_js:
  - "12"
dist: trusty
sudo: required
branches:
  only:
    - main
before_script:
  - npm install -g @angular/cli
script:
  - ng lint
  - ng serve
  - pa11y-ci -c ./.pa11yci
# deploy:
#   provider: pages
#   skip_cleanup: true
#   github_token: $GITHUB_TOKEN
#   local_dir: dist/techdemo-ng-universal-leaflet
#   on:
#     branch: main
